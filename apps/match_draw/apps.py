from django.apps import AppConfig


class MatchDrawConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    # name = 'match_draw'
    name = "apps.match_draw"
    verbose_name = "Match Draw"
